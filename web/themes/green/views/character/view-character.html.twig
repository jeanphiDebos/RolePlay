{% extends "@green/layout-view.html.twig" %}

{% block quick_shortcut_table_container %}
    {% include "@green/character/quick-shortcut-character.html.twig" %}
{% endblock quick_shortcut_table_container %}

{% block action_table_container %}
    {% include "@green/character/action-table-character.html.twig" %}
{% endblock action_table_container %}

{% block entity_view_container %}
    <div class="panel-body">
        <div class="well">
            <dl class="dl-horizontal">
                <dt>{{ 'label.id'|trans }}</dt>
                <dd>{{ entity.id }}</dd>
                <dt>{{ 'label.name'|trans }}</dt>
                <dd>{{ entity.name }}</dd>
                <dt>{{ 'label.universe'|trans }}</dt>
                <dd>{{ entity.universe.name|default('label.na'|trans) }}</dd>
            </dl>
        </div>
    </div>
{% endblock entity_view_container %}

{% block extra_entity_view_container %}
    {% embed "@green/templates/table-panel.html.twig" %}
        {% block table_panel_title %}{{ 'label.configuration_fields'|trans }}{% endblock %}
        {% block table_panel_th %}
            <th>#</th>
            <th>{{ 'label.name'|trans }}</th>
            <th>{{ 'label.value'|trans }}</th>
            <th>{{ 'label.description'|trans }}</th>
            <th>{{ 'label.type'|trans }}</th>
        {% endblock %}
        {% block table_panel_tr %}
            {% for fieldCharacter in entity.fieldCharacters %}
                <tr>
                    <td><a href="{{ path('appbundle_configuration_field_view',{'id':fieldCharacter.configurationField.id}) }}">{{ fieldCharacter.configurationField.id }}</a></td>
                    <td><a href="{{ path('appbundle_configuration_field_view',{'id':fieldCharacter.configurationField.id}) }}">{{ fieldCharacter.configurationField.name }}</a></td>
                    <td>{{ fieldCharacter.value|default('label.na'|trans) }}</td>
                    <td>{{ fieldCharacter.configurationField.description|default('label.na'|trans) }}</td>
                    <td>{{ fieldCharacter.configurationField.type.name|default('label.na'|trans) }}</td>
                </tr>
            {% endfor %}
        {% endblock %}
    {% endembed %}
    {% include "@green/whisper/listing-whisper.html.twig" %}
{% endblock %}